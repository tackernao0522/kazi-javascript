// Logical Operators
// and (&&), or (||), not (!)

let price = 10;

console.log(price > 5 && price < 15); // true
// true && true

console.log(price > 50 || price < 15); // true
// false || true

console.log(!(price > 50)); // true
